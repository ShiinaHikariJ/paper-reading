论文定位

类型： 时域、频域变换增强＋自监督对比预训练。

解决问题： 不同时间序列数据集间分布差异大，普通预训练模型跨数据集迁移能力不足。

核心假设： 同一条时序信号的时域表示和频域表示在潜在空间中应保持接近。

方法流程
原始时序
├─ 时域增强 → 时域编码器
└─ FFT → 频域增强 → 频域编码器
             ↓
       时频表示对齐
             ↓
       无标签预训练模型
             ↓
       目标数据少量标签微调
三个损失
L
T
	​

：保持时域增强前后表示一致；
L
F
	​

：保持频域增强前后表示一致；
L
C
	​

：保持同一样本时域与频域表示一致。
增强操作

时域： 加噪、缩放、平移、相邻片段。

频域： 删除少量频率分量、增加少量频率分量。

验证方式
跨数据集预训练与微调；
轴承FD-A到FD-B跨工况实验；
与随机初始化和多种自监督方法比较；
分类、聚类和异常检测；
三个损失的消融实验。
主要结论

时频一致性预训练能够提高少样本目标数据集上的分类和迁移性能。

对轴承研究的启发

时域和频域联合增强有价值，尤其适合跨工况表示学习。

潜在问题

随机频率扰动可能破坏故障特征频率，缺少机械机理约束。

可能的后续问题

如何设计故障特征保持的时频增强，使模型在跨工况下获得更强泛化能力？